import assert from 'node:assert';
import { describe, it } from 'node:test';

describe('cogito cache (placeholder)', () => {
  it('sanity: null is null', () => {
    assert.strictEqual(null, null);
  });
});
